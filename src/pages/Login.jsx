import React from 'react';

const Login = () => {

    return (
        <div>
            <div>
                <button >Login</button>
                <button onClick={null}>Enter with Google</button>
            </div>
        </div>
    )
}

export default Login